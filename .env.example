APP_NAME="DeSnipperaar Admin"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://admin.desnipperaar.nl

APP_LOCALE=nl
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=nl_NL

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

DB_CONNECTION=pgsql
DB_HOST=188.245.57.103
DB_PORT=5432
DB_DATABASE=desnipperaar
DB_USERNAME=u_c30ead05
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database

MAIL_MAILER=resend
RESEND_API_KEY=
MAIL_FROM_ADDRESS="sales@desnipperaar.nl"
MAIL_FROM_NAME="DeSnipperaar"

# Customer-facing mails (OrderCreated, QuoteRequested) are sent From sales@ and BCC'd to
# ADMIN_NOTIFY_EMAIL so the team inbox keeps a copy.
SALES_EMAIL=sales@desnipperaar.nl
ADMIN_NOTIFY_EMAIL=sales@desnipperaar.nl

DESNIPPERAAR_ORDER_PREFIX=B
DESNIPPERAAR_QUOTE_PREFIX=O
DESNIPPERAAR_ORDER_START=142
DESNIPPERAAR_BON_PREFIX=P
DESNIPPERAAR_CERT_PREFIX=C

# Group deals — global settings. See docs/group-deals.md.
GROUP_DEAL_ORGANIZER_PERK=first_box_free
GROUP_DEAL_MAX_JOINERS=30
GROUP_DEAL_JOIN_CUTOFF_DAYS=2
GROUP_DEAL_MIN_HORIZON_DAYS=7
GROUP_DEAL_MAX_HORIZON_DAYS=90
GROUP_DEAL_ONE_PER_CITY_PER_DAY=true
# Floor the organizer's group target so the smallest deal stays economically
# viable. Backend rejects lower values with a 422; the SSR start form sets the
# input min attribute from this value too.
GROUP_DEAL_MIN_TARGET_BOXES=1
GROUP_DEAL_MIN_TARGET_CONTAINERS=0

# Secret shared with GitHub webhook so POST /api/deploy works.
DEPLOY_HOOK_SECRET=

VITE_APP_NAME="${APP_NAME}"
